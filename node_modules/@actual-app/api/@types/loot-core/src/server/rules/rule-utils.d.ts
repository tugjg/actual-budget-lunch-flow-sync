import { RSchedule } from '../util/rschedule';
import type { Rule } from './rule';
export declare function assert(test: unknown, type: string, msg: string): asserts test;
export declare function rankRules(rules: Iterable<Rule>): Rule[];
export declare function migrateIds(rule: Rule, mappings: Map<string, string>): void;
export declare function iterateIds(rules: Rule[], fieldName: string, func: (rule: Rule, id: string) => void | boolean): void;
export declare function parseRecurDate(desc: any): {
    type: string;
    schedule: RSchedule;
};
export declare function parseDateString(str: any): {
    type: string;
    date: string;
};
export declare function parseBetweenAmount(between: any): {
    type: string;
    num1: number;
    num2: number;
};
