import type { CategoryEntity } from '../../types/models';
import type { Template } from '../../types/models/templates';
type Notification = {
    type?: 'message' | 'error' | 'warning' | undefined;
    pre?: string | undefined;
    title?: string | undefined;
    message: string;
    sticky?: boolean | undefined;
};
export declare function storeTemplates({ categoriesWithTemplates, source, }: {
    categoriesWithTemplates: {
        id: string;
        templates: Template[];
    }[];
    source: 'notes' | 'ui';
}): Promise<void>;
export declare function applyTemplate({ month, }: {
    month: string;
}): Promise<Notification>;
export declare function overwriteTemplate({ month, }: {
    month: string;
}): Promise<Notification>;
export declare function applyMultipleCategoryTemplates({ month, categoryIds, }: {
    month: string;
    categoryIds: Array<CategoryEntity['id']>;
}): Promise<Notification>;
export declare function applySingleCategoryTemplate({ month, category, }: {
    month: string;
    category: CategoryEntity['id'];
}): Promise<Notification>;
export declare function runCheckTemplates(): Promise<{
    type?: "message" | "error" | "warning" | undefined;
    pre?: string | undefined;
    message: string;
    sticky?: boolean | undefined;
}>;
export declare function getTemplatesForCategory(categoryId: CategoryEntity['id']): Promise<Record<CategoryEntity['id'], Template[]>>;
export {};
