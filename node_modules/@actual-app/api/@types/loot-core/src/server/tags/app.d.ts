import type { TagEntity } from '../../types/models';
export type TagsHandlers = {
    'tags-get': typeof getTags;
    'tags-create': typeof createTag;
    'tags-delete': typeof deleteTag;
    'tags-delete-all': typeof deleteAllTags;
    'tags-update': typeof updateTag;
    'tags-discover': typeof discoverTags;
};
export declare const app: {
    events: import("mitt").Emitter<{
        sync: import("../../types/server-events").ServerEvents["sync-event"];
        'load-budget': {
            id: string;
        };
    }>;
    handlers: TagsHandlers;
    services: (() => () => void)[];
    unlistenServices: (() => void)[];
    method<Name extends "tags-get" | "tags-create" | "tags-delete" | "tags-delete-all" | "tags-update" | "tags-discover">(name: Name, func: TagsHandlers[Name]): void;
    service(func: () => () => void): void;
    combine(...apps: any[]): void;
    startServices(): void;
    stopServices(): Promise<void>;
};
declare function getTags(): Promise<TagEntity[]>;
declare function createTag({ tag, color, description, }: Omit<TagEntity, 'id'>): Promise<TagEntity>;
declare function deleteTag(tag: Pick<TagEntity, 'id'>): Promise<TagEntity['id']>;
declare function deleteAllTags(ids: Array<TagEntity['id']>): Promise<Array<TagEntity['id']>>;
declare function updateTag(tag: Partial<TagEntity> & Pick<TagEntity, 'id'>): Promise<Partial<TagEntity>>;
declare function discoverTags(): Promise<TagEntity[]>;
export {};
