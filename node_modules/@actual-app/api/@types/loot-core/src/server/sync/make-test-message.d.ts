export declare function makeTestMessage(keyId: any): Promise<{
    value: Buffer<ArrayBuffer>;
    meta: {
        keyId: any;
        algorithm: string;
        iv: string;
        authTag: string;
    };
}>;
