import type { NoteEntity } from '../../types/models';
export type NotesHandlers = {
    'notes-save': typeof updateNotes;
    'notes-save-undoable': typeof updateNotes;
};
export declare const app: {
    events: import("mitt").Emitter<{
        sync: import("../../types/server-events").ServerEvents["sync-event"];
        'load-budget': {
            id: string;
        };
    }>;
    handlers: NotesHandlers;
    services: (() => () => void)[];
    unlistenServices: (() => void)[];
    method<Name extends "notes-save" | "notes-save-undoable">(name: Name, func: NotesHandlers[Name]): void;
    service(func: () => () => void): void;
    combine(...apps: any[]): void;
    startServices(): void;
    stopServices(): Promise<void>;
};
declare function updateNotes({ id, note }: NoteEntity): Promise<void>;
export {};
